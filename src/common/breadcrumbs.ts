import {Breadcrumbs} from 'common/types';

const breadcrumbs: Breadcrumbs = [
    {
        path: '/redux',
        childs: ['redux_demo'],
    }, {
        path: '/mobx',
        childs: ['mobx_demo'],
    },
];

export default breadcrumbs;
